# Lima และ Colima

## ภาพรวม (Overview)

Repository นี้ให้ข้อมูลเกี่ยวกับ Lima และ Colima ซึ่งเป็นเครื่องมือสำหรับรัน Linux Virtual Machines บน macOS และระบบปฏิบัติการอื่นๆ

## Lima คืออะไร?

**Lima (Linux Machines)** เป็นโปรเจกต์ open-source ที่ช่วยให้คุณสามารถรัน Linux Virtual Machines บน macOS ได้อย่างง่ายดาย โดยมีการ auto file sharing, port forwarding และ containerd ในตัว

### คุณสมบัติหลักของ Lima:

- **รัน Linux VMs**: สามารถสร้างและจัดการ Linux virtual machines ได้ง่าย
- **File Sharing อัตโนมัติ**: แชร์ไฟล์ระหว่าง host กับ VM ได้โดยอัตโนมัติ
- **Port Forwarding**: รองรับการ forward ports อัตโนมัติ
- **รองรับหลาย architectures**: ทำงานได้ทั้ง x86_64 และ ARM64
- **Built-in Containerd**: มี container runtime ในตัว
- **Template YAML**: กำหนดค่า VM ผ่านไฟล์ YAML ที่ยืดหยุ่น

### การใช้งาน Lima:

```bash
# ติดตั้ง Lima
brew install lima

# สร้าง VM ใหม่
limactl start

# เข้าใช้งาน VM
lima

# หยุด VM
limactl stop
```

## Colima คืออะไร?

**Colima** ย่อมาจาก **Container runtimes on Lima** เป็นเครื่องมือที่สร้างมาจาก Lima โดยเฉพาะเพื่อรัน container runtimes (Docker และ Kubernetes) บน macOS ด้วย minimal setup

### คุณสมบัติหลักของ Colima:

- **ทดแทน Docker Desktop**: ใช้เป็นทางเลือกแทน Docker Desktop บน macOS
- **รองรับ Docker และ Kubernetes**: สามารถรัน Docker containers และ Kubernetes clusters
- **ใช้ทรัพยากรน้อย**: ใช้ memory และ CPU น้อยกว่า Docker Desktop
- **ฟรีและ Open Source**: ไม่มีค่าใช้จ่ายและไม่มีข้อจำกัดด้านลิขสิทธิ์
- **รองรับหลาย Container Runtimes**: รองรับทั้ง Docker, Containerd และ Kubernetes

### การใช้งาน Colima:

```bash
# ติดตั้ง Colima
brew install colima

# เริ่มต้น Colima
colima start

# เริ่มต้นพร้อม Kubernetes
colima start --kubernetes

# ตั้งค่า CPU และ Memory
colima start --cpu 4 --memory 8

# ตรวจสอบสถานะ
colima status

# หยุด Colima
colima stop
```

### ใช้ Docker กับ Colima:

เมื่อเริ่ม Colima แล้ว คุณสามารถใช้คำสั่ง Docker ได้ตามปกติ:

```bash
docker run hello-world
docker ps
docker images
```

## ความแตกต่างระหว่าง Lima และ Colima

| คุณสมบัติ | Lima | Colima |
|---------|------|--------|
| **วัตถุประสงค์** | รัน Linux VMs แบบทั่วไป | รัน Container runtimes โดยเฉพาะ |
| **ความซับซ้อน** | ต้องการการตั้งค่ามากกว่า | พร้อมใช้งานทันที (opinionated) |
| **กรณีการใช้งาน** | ยืดหยุ่น, ปรับแต่งได้ | Docker/Kubernetes โดยเฉพาะ |
| **Target Users** | ผู้ใช้ทั่วไปที่ต้องการ Linux VM | นักพัฒนาที่ต้องการ container runtime |

## ข้อดีของการใช้ Lima/Colima

1. **ประหยัดทรัพยากร**: ใช้ memory และ CPU น้อยกว่า Docker Desktop
2. **ฟรีและ Open Source**: ไม่มีค่าใช้จ่ายสำหรับองค์กร
3. **รองรับ Apple Silicon**: ทำงานได้ดีบน M1/M2/M3 Macs
4. **เบาและรวดเร็ว**: เริ่มต้นเร็วกว่า Docker Desktop
5. **ยืดหยุ่น**: ปรับแต่งได้ตามความต้องการ

## ข้อจำกัด

- รองรับเฉพาพ macOS และ Linux (ไม่รองรับ Windows โดยตรง)
- UI น้อยกว่า Docker Desktop (ใช้งานผ่าน CLI เป็นหลัก)
- Community support อาจน้อยกว่า Docker Desktop

## เมื่อไหร่ควรใช้อะไร?

- **ใช้ Lima** เมื่อ: ต้องการ Linux VM ที่ปรับแต่งได้, ทดสอบ Linux distributions ต่างๆ, หรือต้องการ environment ที่ยืดหยุ่น
- **ใช้ Colima** เมื่อ: ต้องการรัน Docker containers, ต้องการทางเลือกแทน Docker Desktop, หรือต้องการ Kubernetes cluster สำหรับพัฒนา

## ตัวอย่างและเอกสารในโปรเจกต์

Repository นี้มีตัวอย่างและเอกสารแนะนำการใช้งานในโฟลเดอร์ต่างๆ:

### 📁 Lima Examples

- **[lima/cli-examples.md](lima/cli-examples.md)** - ตัวอย่างคำสั่ง Lima CLI ครบถ้วน พร้อมคำอธิบายภาษาไทย
  - การติดตั้งและจัดการ VM
  - การเข้าใช้งานและรันคำสั่ง
  - การทำงานกับ Containers
  - Port Forwarding และ File Sharing
  - Debug และ Troubleshooting

- **[lima/example-configs.yaml](lima/example-configs.yaml)** - ตัวอย่าง YAML Configuration Files พร้อมคำอธิบาย
  - Basic Ubuntu VM
  - Docker Development VM
  - Kubernetes Development VM
  - Minimal VM สำหรับ Testing
  - Development VM พร้อม Tools
  - และอื่นๆ อีกมากมาย

### 📁 Colima Examples

- **[colima/cli-examples.md](colima/cli-examples.md)** - ตัวอย่างคำสั่ง Colima CLI ครบถ้วน พร้อมคำอธิบายภาษาไทย
  - การติดตั้งและเริ่มต้นใช้งาน
  - การทำงานกับ Docker
  - การทำงานกับ Kubernetes
  - การตั้งค่าและปรับแต่ง
  - Performance Tuning
  - Troubleshooting

- **[colima/example-configs.yaml](colima/example-configs.yaml)** - ตัวอย่าง Colima Configuration Files พร้อมคำอธิบาย
  - Basic Docker Development
  - Performance Optimized Configuration
  - Kubernetes Development
  - Multi-Architecture Build
  - Full-featured Development Environment
  - CI/CD Simulation
  - และอื่นๆ

## วิธีใช้งานตัวอย่าง

### สำหรับ Lima:

```bash
# ใช้ config file ตัวอย่าง
limactl start --name=my-vm lima/example-configs.yaml

# หรือ copy และปรับแต่งตามต้องการ
cp lima/example-configs.yaml my-custom-vm.yaml
# แก้ไข my-custom-vm.yaml ตามต้องการ
limactl start --name=custom my-custom-vm.yaml
```

### สำหรับ Colima:

```bash
# แก้ไข config ของ Colima
colima edit

# จากนั้น copy config จากตัวอย่างใน colima/example-configs.yaml
# ไปใส่ในไฟล์ที่เปิดขึ้นมา

# หรือสร้าง profile ใหม่
colima start --profile development --edit
# จากนั้น copy config จากตัวอย่าง
```

## แหล่งข้อมูลเพิ่มเติม

- [Lima GitHub](https://github.com/lima-vm/lima)
- [Colima GitHub](https://github.com/abiosoft/colima)
- [Lima Documentation](https://lima-vm.io/)

## License

โปรดดูไฟล์ LICENSE ในแต่ละโปรเจกต์